# Qboxai — Teacher-enabled Education Dashboard (Frontend)

This is a minimal React (Vite) app you can run in VS Code.

## Features
- Attendance removed
- Teacher tab to assign MCQs, Flashcards, Interview questions by topic
- Student & Parent views show mastery only
- School Admin sees overview and latest assignments

## Run locally
```bash
npm install
npm run dev
# open the local URL shown in terminal
```
